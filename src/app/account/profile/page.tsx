'use client';
import Link from'next/link';import{FormEvent,useEffect,useState}from'react';import{emptyCustomerProfile,getCustomerProfile,saveCustomerProfile,type CustomerProfile}from'@/lib/customer-ui-utils';
export default function CustomerProfilePage(){const[profile,setProfile]=useState<CustomerProfile>(emptyCustomerProfile);const[saved,setSaved]=useState(false);useEffect(()=>setProfile(getCustomerProfile()),[]);function submit(e:FormEvent){e.preventDefault();saveCustomerProfile(profile);setSaved(true);window.setTimeout(()=>setSaved(false),1800)}return <main className="min-h-screen bg-[#fbf7ef] px-6 py-16 text-stone-950 dark:bg-[#0f0d0a] dark:text-white"><div className="mx-auto max-w-3xl"><Link href="/account" className="font-bold text-amber-800">← Back to account</Link><h1 className="mt-8 text-5xl font-black">Customer profile</h1><p className="mt-3 text-stone-600 dark:text-white/60">Save your shipping details locally so checkout is faster. This is a free feature and does not require a paid third-party service.</p><form onSubmit={submit} className="mt-8 rounded-[2rem] bg-white p-6 shadow-sm dark:bg-white/5"><div className="grid gap-4 md:grid-cols-2"><Field label="Full name" value={profile.fullName} onChange={v=>setProfile({...profile,fullName:v})}/><Field label="Phone / mobile" value={profile.phone} onChange={v=>setProfile({...profile,phone:v})}/><Field label="City" value={profile.city} onChange={v=>setProfile({...profile,city:v})}/><Field label="Province" value={profile.province} onChange={v=>setProfile({...profile,province:v})}/></div><label className="mt-4 block text-sm font-black text-stone-500 dark:text-white/50">Complete address<textarea value={profile.address} onChange={e=>setProfile({...profile,address:e.target.value})} className="mt-2 h-24 w-full rounded-2xl border border-stone-200 bg-white px-4 py-3 text-stone-950 outline-none dark:border-white/10 dark:bg-black/20 dark:text-white"/></label><label className="mt-4 block text-sm font-black text-stone-500 dark:text-white/50">Delivery notes<textarea value={profile.notes} onChange={e=>setProfile({...profile,notes:e.target.value})} className="mt-2 h-20 w-full rounded-2xl border border-stone-200 bg-white px-4 py-3 text-stone-950 outline-none dark:border-white/10 dark:bg-black/20 dark:text-white"/></label><button className="mt-6 rounded-full bg-amber-800 px-7 py-4 font-black text-white transition hover:bg-amber-700">{saved?'Saved ✓':'Save profile'}</button></form></div></main>}
function Field({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block text-sm font-black text-stone-500 dark:text-white/50">{label}<input value={value} onChange={e=>onChange(e.target.value)} className="mt-2 w-full rounded-2xl border border-stone-200 bg-white px-4 py-3 text-stone-950 outline-none dark:border-white/10 dark:bg-black/20 dark:text-white"/></label>}
