'use client';
import { FormEvent, useEffect, useState } from 'react';
import Link from 'next/link';
import { useProducts } from '@/context/ProductContext';
import { supabase } from '@/lib/supabase';

export default function ReviewsPage(){const{activeProducts}=useProducts();const[reviews,setReviews]=useState<any[]>([]);const[msg,setMsg]=useState('');async function load(){if(!supabase)return;const{data}=await supabase.from('reviews').select('*').order('created_at',{ascending:false}).limit(30);setReviews(data||[])}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(!supabase){setMsg('Supabase is not connected.');return;}const{error}=await supabase.from('reviews').insert({product_id:f.get('product_id'),customer_name:f.get('customer_name'),rating:Number(f.get('rating')),title:f.get('title'),body:f.get('body'),approved:true});setMsg(error?error.message:'Review submitted.');if(!error){(e.currentTarget as HTMLFormElement).reset();load();}}return <main className="min-h-screen bg-[#fbf7ef] px-6 py-16 text-stone-950 dark:bg-[#0f0d0a] dark:text-white"><div className="mx-auto max-w-5xl"><Link href="/products" className="font-bold text-amber-800">← Back to products</Link><h1 className="mt-8 text-5xl font-black">Customer reviews</h1><form onSubmit={submit} className="mt-8 grid gap-4 rounded-[2rem] bg-white p-6 shadow-sm dark:bg-white/5"><select name="product_id" required className="rounded-2xl border border-stone-200 px-5 py-4 dark:border-white/10 dark:bg-black/20">{activeProducts.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input name="customer_name" required placeholder="Your name" className="rounded-2xl border border-stone-200 px-5 py-4 dark:border-white/10 dark:bg-black/20"/><select name="rating" className="rounded-2xl border border-stone-200 px-5 py-4 dark:border-white/10 dark:bg-black/20"><option value="5">5 stars</option><option value="4">4 stars</option><option value="3">3 stars</option></select><input name="title" required placeholder="Review title" className="rounded-2xl border border-stone-200 px-5 py-4 dark:border-white/10 dark:bg-black/20"/><textarea name="body" required placeholder="Your review" className="h-28 rounded-2xl border border-stone-200 px-5 py-4 dark:border-white/10 dark:bg-black/20"/><button className="rounded-full bg-amber-800 px-6 py-3 font-black text-white">Submit review</button>{msg&&<p className="font-bold text-amber-800">{msg}</p>}</form><div className="mt-8 grid gap-4">{reviews.map(r=><article key={r.id} className="rounded-2xl bg-white p-5 shadow-sm dark:bg-white/5"><p className="font-black">{r.title} <span className="text-amber-700">★ {r.rating}</span></p><p className="mt-1 text-sm text-stone-500 dark:text-white/50">{r.customer_name}</p><p className="mt-3 text-stone-700 dark:text-white/70">{r.body}</p></article>)}</div></div></main>}
