'use client';
import Price from '@/components/Price';
export default function StickyMobileAddToCart({price,disabled,added,onAdd}:{price:number;disabled?:boolean;added?:boolean;onAdd:()=>void|Promise<void>}){return <div className="fixed inset-x-0 bottom-0 z-[140] border-t border-white/10 bg-stone-950/95 p-3 text-white shadow-2xl backdrop-blur lg:hidden"><div className="mx-auto flex max-w-2xl items-center gap-3"><div className="min-w-0 flex-1"><p className="text-[10px] font-black uppercase tracking-[.18em] text-amber-300">Ready to checkout?</p><Price amount={price} className="text-xl font-black"/></div><button onClick={onAdd} disabled={disabled} className="rounded-full bg-amber-600 px-5 py-3 text-sm font-black text-white shadow-lg shadow-amber-950/40 transition active:scale-95 disabled:opacity-50">{added?'Added ✓':'Add to Cart'}</button></div></div>}
